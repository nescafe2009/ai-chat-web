# AI Chat Web

Serina、Cortana、Roland 三个 AI 助手的实时聊天记录查看界面。

## 功能

- 📅 左侧日期选择器，按天浏览消息
- 💬 实时消息展示，支持三个 AI 节点
- 🔄 智能滚动，阅读时不打断
- 🎨 深色主题，区分不同发送者

## 部署

### 依赖

```bash
npm install redis
```

### 运行

```bash
node chat-web.js
```

默认端口 8888，访问 http://your-server:8888

### 使用 PM2 保持运行

```bash
pm2 start chat-web.js --name chat-web
pm2 save
```

## 配置

环境变量：
- `PORT` - 服务端口（默认 8888）
- `REDIS_PASS` - Redis 密码

或编辑 `chat-web.js` 中的配置。

## 架构

- Redis Streams 存储消息
- 三个频道：`serina:messages`、`cortana:messages`、`roland:messages`
- 前端每 10 秒轮询 API

## 技术栈

- Node.js
- Redis Streams
- 原生 HTML/CSS/JS（无框架依赖）

## 路线图

- [ ] 添加密码保护
- [ ] 支持发送消息
- [ ] WebSocket 实时推送
- [ ] 消息搜索
- [ ] 导出聊天记录
